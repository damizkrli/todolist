<nav class="navbar navbar-light bg-light">
    <div class="container-fluid">
        {% if app.user %}
            <a class="navbar-brand">Welcome Back, {{ app.user.firstname }} {{ app.user.lastname }} 🖖</a>
        {% else %}
            <a class="navbar-brand">Welcome Back, don't forget to Log In 👉</a>
        {% endif %}
        <div class="d-flex">
            {% if app.user %}
                <a class="btn btn-danger btn-sm" href="{{ path('app_logout') }}">Logout</a>
            {% else %}
                <a class="btn btn-success btn-sm me-3" href="{{ path('app_login') }}">Login</a>
                <a class="btn btn-info btn-sm" href="{{ path('app_register') }}">Create Account</a>
            {% endif %}
        </div>
    </div>
</nav>